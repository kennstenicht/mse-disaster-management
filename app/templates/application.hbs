{{mapbox-gl-map
  settings=baseMapSettings
  createLence='createLence'
  setBaseMap='setBaseMap'
}}

<!-- {{#each lence in lences}}
  {{#lence-overlay settings=lence}}
    {{mapbox-gl-map settings=lence}}
  {{/lence-overlay}}
{{/each}} -->


{{outlet}}

