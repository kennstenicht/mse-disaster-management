{{#each map in maps}}
  {{#if map.baseMap}}
    {{mapbox-gl-map settings=map tasks=tasks}}
  {{else}}
    {{#if map.active}}
      {{lence-overlay settings=map tasks=tasks isVisible=map.active}}
    {{/if}}
  {{/if}}
{{/each}}

{{outlet}}
